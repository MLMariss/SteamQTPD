# SteamQHPP — Data Coverage Snapshot

> Generated by `coverage.py` after each scrape. Do not edit by hand — changes will be overwritten on the next run. Design rationale for both axes is in ARCHITECTURE §11.5.

**Snapshot generated (UTC):** 2026-07-16 14:15
**Base universe (`games.json`):** 123,623 games

Per-file generation timestamps at snapshot time:

| File | Generated (UTC) |
|---|---|
| `games.json` | 2026-07-16 14:10 |
| `prices.json` | 2026-07-16 11:56 |
| `hltb.json` | 2026-07-16 14:05 |
| `recent.json` | 2026-07-16 12:08 |
| `playtime.json` | 2026-07-16 12:04 |
| `ratings.json` | 2026-07-16 12:04 |
| `tags.json` | 2026-07-16 08:44 |
| `updates.json` | 2026-07-16 11:24 |
| `playtime_raw/` (shards) | newest 2026-07-16 12:03 · oldest 2026-07-08 01:23 |
| `updates_raw/` (41/41 populated) | newest 2026-07-16 11:24 · oldest 2026-07-08 07:05 |
| `pics_raw/` (64/64 populated) | newest 2026-07-16 12:09 · oldest 2026-07-16 12:08 |

---

## Axis 1 — Coverage by metric (sorted by % of catalog, descending)

How much of the catalog we hold for each metric. "Have we got it?"

| Metric | Storage file | Covered | % of catalog | Missing |
|---|---|---:|---:|---:|
| Release date | `games.json` | 123,621 | 100.0% | 2 |
| Recent reviews | `recent.json` | 123,596 | 100.0% | 27 |
| HLTB (total) | `hltb.json` | 123,596 | 100.0% | 27 |
| PICS metadata (raw) | `pics_raw/` | 123,582 | 100.0% | 41 |
| PICS metadata (summ.) | `pics/` | 123,582 | 100.0% | 41 |
| Tags (non-empty) | `tags.json` | 123,562 | 100.0% | 61 |
| Review count | `games.json` | 115,668 | 93.6% | 7,955 |
| Rating % | `games.json` | 112,359 | 90.9% | 11,264 |
| Price / Sales | `prices.json` | 105,461 | 85.3% | — |
| HLTB (real) | `hltb.json` | 104,830 | 84.8% | — |
| Playtime raw (shards) | `playtime_raw/` | 78,648 | 63.6% | 44,975 |
| Playtime (summarized) | `playtime.json` | 78,476 | 63.5% | 45,147 |
| Playtime-weighted rating | `ratings.json` | 78,470 | 63.5% | 45,153 |
| Update events (raw) | `updates_raw/` | 50,221 | 40.6% | 73,402 |
| Update events (summ.) | `updates.json` | 32,570 | 26.3% | 91,053 |
| HLTB (estimated) | `hltb.json` | 18,766 | 15.2% | — |

Integrity: every storage file is effectively a clean subset of `games.json` (a handful of orphan keys can appear as timing artifacts between scraper commits).

---

## Axis 2 — Refresh schedule (is the pipeline keeping up?)

Covered rows split by how each row's **own scraper** will treat it on the next pass — NOT against a flat target. The **7d (active)** lane is games updated within 90d (short cooldown); the **30d (dormant)** lane is everyone else (long cooldown, refreshed rarely *by design*). Track totals **include** their overdue members. **overdue** = already past its lane's cooldown = real backlog. **never** = no data yet = fill frontier. **empty** = correctly skipped (below the 10-review floor / null score), not pending work.

| Metric | Storage | 7d-track | 30d-track | overdue | empty | never |
|---|---|---:|---:|---:|---:|---:|
| Recent reviews | `recent.json` | 10,912 | 112,684 | 18 | 0 | 27 |
| Playtime raw (approx) | `playtime_raw/` | 7,316 | 71,163 | 46,960 † | 44,868 | 276 |
| Update events | `updates_raw/` | 4,689 | 45,532 | 1,031 | 44,868 | 28,534 |

**†  Playtime `overdue` is proxy-inflated — read with caution.** `playtime_raw/` stores no per-game scrape timestamp, so staleness uses each game's **newest review `ts`** as a proxy. A game with no recent reviews reads as overdue even if the scraper walked it days ago, so this figure is an **upper bound**, not true backlog (contrast Update events, which has a real per-game `scraped_at` and shows exact overdue). The `empty` column is the 44,868 games below the 10-review floor (correctly skipped, not backlog). An exact figure needs a per-game `scraped_at` in the shards — see Future work.

Files with a single-window (not two-track) refresh rule:

- **`games.json` core** (`scraper.py`): fresh 11,871 · overdue 111,752 · never 0. Refresh is driven by Steam's `last_modified` in Actions; the overdue count here is the local fallback-timer view (≥7d since `scraped_at`).
- **`hltb.json`** (`hltb_refresh.py`): fresh 33,197 · overdue 24,674 · blank-frozen 0 · blank-active 65,725. Windows: partial 14d, full 365d; blanks back off 3→30→180d by attempt count.
- **`prices.json`** (`price_and_sale.py`): no cooldown gate — the whole non-free base is re-batched every 3h, so there is no meaningful staleness backlog to bucket.
- **`tags.json`** (`tags_refresh.py`): **no timestamp stored and no rescrape schedule** — coverage-only. See Future work below.
- **`playtime.json` / `ratings.json`**: derived from `playtime_raw/` on every raw pass; staleness inherits from the raw shard row above.
- **`pics_raw/`** (`pics_refresh.py`): fresh 123,582 · overdue 0 · never 41. Single flat `--stale-days 14` window (daily cron), and — unlike playtime — keyed on a **real per-game `_ts` fetch stamp**, so overdue here is exact, not a proxy. `never` = catalog games not yet in `pics_raw/` (the fill frontier). `pics/` is derived from `pics_raw/` by `pics_summarize.py` on every pass, so its staleness inherits from this row.

---

## PICS metadata — sub-metric coverage

Fill rates **within the 123,582 summarized `pics/` records** (not against the full catalog). Structural fields (tags, genres, categories, languages, dev/pub) sit near 100%; the sparse rows below them are **by-design carrier-only** fields — their % is a real-world incidence rate (how many games actually carry an AI disclosure, a Deck rating, a review-bomb adjustment, …), **not** a pipeline gap. These figures are live over the full library — they supersede the 120-game probe sample in `PICS_METADATA_PIPELINE.md §2`.

| Sub-metric | Covered | % of `pics/` | Note |
|---|---:|---:|---|
| primary_genre | 123,582 | 100.0% | genre ID (structural) |
| feature categories | 123,562 | 100.0% | single-player/co-op/cloud/... flags |
| store_tags | 123,520 | 99.9% | ranked community tag IDs |
| genres | 123,457 | 99.9% | genre IDs |
| supported languages | 123,515 | 99.9% | language codes |
| developer | 123,415 | 99.9% | structured dev name(s) |
| publisher | 123,128 | 99.6% | structured publisher name(s) |
| release date | 122,014 | 98.7% | PICS steam_release_date |
| release state | 122,132 | 98.8% | released/prerelease (structural) |
| review score | 79,098 | 64.0% | Valve bucket + %positive (review-floor bound) |
| full-audio languages | 54,387 | 44.0% | carriers only |
| controller support | 42,326 | 34.2% | full/partial (from cats 28/18) |
| Steam Deck compat | 33,700 | 27.3% | Deck-rated titles only |
| franchise | 29,073 | 23.5% | structured franchise name(s) |
| content descriptors | 28,013 | 22.7% | mature flags (adult gate = code 4) |
| Early Access (genre-70) | 11,775 | 9.5% | Valve EA signal (not the SteamSpy tag) |
| AI content disclosure | 12,663 | 10.2% | aicontenttype 1/2 carriers only |
| custom EULA | 10,989 | 8.9% | carriers only |
| original release date | 11,212 | 9.1% | EA->1.0 carriers only |
| metacritic | 4,068 | 3.3% | carriers only |
| family-share excluded | 837 | 0.7% | excluded titles only |
| review-bomb adjusted | 97 | 0.1% | bombed titles only |

**AI content disclosure.** 12,663 games carry an `aicontenttype` flag (10.2% of catalog): **12,257 pre-generated** (shipped assets made with AI) and **406 live-generated** (runtime generation). The other ~90% of `pics/` records carry no disclosure flag. PICS gives the typed flag + category only; the free-text blurb lives on the store page.

**Steam Deck.** Of the 33,700 Deck-rated titles: **8,118 Verified**, **19,432 Playable**, **6,150 Unsupported**. The long tail of the catalog is simply unrated by Valve (no Deck category), which is why Deck coverage is ~27% of `pics/`, not the ~97% the AAA-heavy probe sample suggested.

---

## Notes

**Prices.** `prices.json` holds **105,461 rows (85.3% of catalog)**, tracking the non-free base (**105,491** games after removing 18,132 `is_free`). **13,467 of 105,461 priced titles (12.8%) are currently discounted**; sale end-dates come from the `IStoreBrowseService/GetItems` pass.

**Playtime backfill.** Raw playtime covers **78,648 games (63.6% of catalog)**. The honest denominator is the addressable set after the `MIN_REVIEWS_FLOOR = 10` gate — **78,755 games (63.7% of catalog)** — against which raw is **99.9% of addressable**. The other **44,868 games are below the floor and correctly skipped** (too few reviews to produce a sentiment-split median).

**Summarizers run on every raw pass.** Raw 78,648 · summarized 78,476 · rated 78,470 (current gap 172). `playtime_summarize.py` and `ratings_summarize.py` are chained into `playtime-raw.yml`, so the small frontend files refresh right after the shards do (8×/day). Any residual gap is just within-run ordering between the shard commit and the summarize steps, not a stalled pipeline.

**Update events.** `updates.json` (event_type-based patch history) covers **32,570 games (26.3%)**, built from **41 of 41 `updates_raw/` shards populated**. This layer feeds the frontend's **Updated column cadence badge** (`N · 90d` / `N · 1y`, from the summed 90d/365d `counts`) and backfills a null News-API `last_update_ts`. It is gated by the same 10-review floor as playtime. As the remaining shards populate this coverage rises; the precedence flip (event layer becomes primary over News-API) is gated on that coverage — see ARCHITECTURE §9.5 / §3.1.

**HLTB estimates refresh live.** **18,766 of 123,596 `hltb.json` entries carry the `est` flag** (15.2%), leaving **104,830 reals (84.8%)**. `hltb_estimate.py` is a shared helper imported by `hltb_refresh.py` (as `HE`) and called in the fill loop, so estimates recompute from live median ratios every 2h as new reals land — not a stale one-off.

**Rating % / review count.** Rating % is present for **112,359 games (90.9%)**; review count for **115,668 (93.6%)**. The gap is titles with too few reviews to carry a meaningful score.

**Tags** are non-empty for **123,562 games (100.0%)**. The remainder are untagged on SteamSpy (typically very-low-review or unreleased titles).

---

## Future work

- **Tag refresh schedule.** `tags.json` currently has no per-entry timestamp and no rescrape cadence — tags are fetched once and left. Tags do drift (Steam user-tag votes shift, new tags get added), so a light periodic re-check (e.g. weekly/monthly, oldest-first, small budget) is worth adding. That needs a per-entry `scraped_at` in `tags.json` first, after which tags can join Axis 2 as a proper row here.
- **Uniform-refresh review.** Once the fill frontier drains (near-100% coverage), revisit whether the dormant 30–45d cooldowns should tighten toward a flatter, faster whole-catalog refresh. Today's split (active 4–7d / dormant 30–45d) deliberately front-loads budget onto games whose data actually moves; a uniform target only makes sense once no backlog is competing for that budget.
- **Playtime per-game timestamp.** Adding a per-game `scraped_at` to the `playtime_raw/` shard records would replace the newest-review-`ts` proxy with an exact staleness signal, removing the (approx) caveat above.

