# SteamQHPP — Data Coverage Snapshot

> Generated by `coverage.py` after each scrape. Do not edit by hand — changes will be overwritten on the next run. Design rationale for both axes is in ARCHITECTURE §11.5.

**Snapshot generated (UTC):** 2026-07-20 14:31
**Base universe (`games.json`):** 123,970 games

Per-file generation timestamps at snapshot time:

| File | Generated (UTC) |
|---|---|
| `games.json` | 2026-07-20 14:25 |
| `prices.json` | 2026-07-20 10:21 |
| `hltb.json` | 2026-07-20 13:24 |
| `recent.json` | 2026-07-20 12:46 |
| `playtime.json` | 2026-07-20 12:38 |
| `ratings.json` | 2026-07-20 12:38 |
| `tags.json` | 2026-07-20 11:24 |
| `updates.json` | 2026-07-20 12:11 |
| `playtime_raw/` (shards) | newest 2026-07-20 12:37 · oldest 2026-07-11 21:43 |
| `updates_raw/` (57/57 populated) | newest 2026-07-20 12:11 · oldest 2026-07-08 07:05 |
| `pics_raw/` (64/64 populated) | newest 2026-07-20 06:28 · oldest 2026-07-17 05:59 |

---

## Axis 1 — Coverage by metric (sorted by % of catalog, descending)

How much of the catalog we hold for each metric. "Have we got it?"

| Metric | Storage file | Covered | % of catalog | Missing |
|---|---|---:|---:|---:|
| Release date | `games.json` | 123,968 | 100.0% | 2 |
| Recent reviews | `recent.json` | 123,948 | 100.0% | 22 |
| HLTB (total) | `hltb.json` | 123,948 | 100.0% | 22 |
| Tags (non-empty) | `tags.json` | 123,914 | 100.0% | 56 |
| PICS metadata (raw) | `pics_raw/` | 123,914 | 100.0% | 56 |
| PICS metadata (summ.) | `pics/` | 123,914 | 100.0% | 56 |
| Review count | `games.json` | 115,859 | 93.5% | 8,111 |
| Rating % | `games.json` | 112,544 | 90.8% | 11,426 |
| Price / Sales | `prices.json` | 105,791 | 85.3% | — |
| HLTB (real) | `hltb.json` | 105,126 | 84.8% | — |
| Playtime raw (shards) | `playtime_raw/` | 78,740 | 63.5% | 45,230 |
| Playtime (summarized) | `playtime.json` | 78,569 | 63.4% | 45,401 |
| Playtime-weighted rating | `ratings.json` | 78,563 | 63.4% | 45,407 |
| Update events (raw) | `updates_raw/` | 69,954 | 56.4% | 54,016 |
| Update events (summ.) | `updates.json` | 45,402 | 36.6% | 78,568 |
| HLTB (estimated) | `hltb.json` | 18,822 | 15.2% | — |

Integrity: every storage file is effectively a clean subset of `games.json` (a handful of orphan keys can appear as timing artifacts between scraper commits).

---

## Axis 2 — Refresh schedule (is the pipeline keeping up?)

Covered rows split by how each row's **own scraper** will treat it on the next pass — NOT against a flat target. The **7d (active)** lane is games updated within 90d (short cooldown); the **30d (dormant)** lane is everyone else (long cooldown, refreshed rarely *by design*). Track totals **include** their overdue members. **overdue** = already past its lane's cooldown = real backlog. **never** = no data yet = fill frontier. **empty** = correctly skipped (below the 10-review floor / null score), not pending work.

| Metric | Storage | 7d-track | 30d-track | overdue | empty | never |
|---|---|---:|---:|---:|---:|---:|
| Recent reviews | `recent.json` | 10,857 | 113,091 | 6 | 0 | 22 |
| Playtime raw (approx) | `playtime_raw/` | 7,250 | 71,322 | 48,909 † | 45,132 | 266 |
| Update events | `updates_raw/` | 6,426 | 63,528 | 3,137 | 45,132 | 8,884 |

**†  Playtime `overdue` is proxy-inflated — read with caution.** `playtime_raw/` stores no per-game scrape timestamp, so staleness uses each game's **newest review `ts`** as a proxy. A game with no recent reviews reads as overdue even if the scraper walked it days ago, so this figure is an **upper bound**, not true backlog (contrast Update events, which has a real per-game `scraped_at` and shows exact overdue). The `empty` column is the 45,132 games below the 10-review floor (correctly skipped, not backlog). An exact figure needs a per-game `scraped_at` in the shards — see Future work.

Files with a single-window (not two-track) refresh rule:

- **`games.json` core** (`scraper.py`): fresh 2,692 · overdue 121,278 · never 0. Refresh is driven by Steam's `last_modified` in Actions; the overdue count here is the local fallback-timer view (≥7d since `scraped_at`).
- **`hltb.json`** (`hltb_refresh.py`): fresh 33,436 · overdue 1 · blank-frozen 0 · blank-active 90,511. Windows: partial 14d, full 365d; blanks back off 3→30→180d by attempt count.
- **`prices.json`** (`price_and_sale.py`): no cooldown gate — the whole non-free base is re-batched every 3h, so there is no meaningful staleness backlog to bucket.
- **`tags.json`** (`tags_refresh.py`): **no timestamp stored and no rescrape schedule** — coverage-only. See Future work below.
- **`playtime.json` / `ratings.json`**: derived from `playtime_raw/` on every raw pass; staleness inherits from the raw shard row above.
- **`pics_raw/`** (`pics_refresh.py`): fresh 123,914 · overdue 0 · never 56. Single flat `--stale-days 14` window (daily cron), and — unlike playtime — keyed on a **real per-game `_ts` fetch stamp**, so overdue here is exact, not a proxy. `never` = catalog games not yet in `pics_raw/` (the fill frontier). `pics/` is derived from `pics_raw/` by `pics_summarize.py` on every pass, so its staleness inherits from this row.

---

## PICS metadata — sub-metric coverage

Fill rates **within the 123,914 summarized `pics/` records** (not against the full catalog). Structural fields (tags, genres, categories, languages, dev/pub) sit near 100%; the sparse rows below them are **by-design carrier-only** fields — their % is a real-world incidence rate (how many games actually carry an AI disclosure, a Deck rating, a review-bomb adjustment, …), **not** a pipeline gap. These figures are live over the full library — they supersede the 120-game probe sample in `PICS_METADATA_PIPELINE.md §2`.

| Sub-metric | Covered | % of `pics/` | Note |
|---|---:|---:|---|
| primary_genre | 123,914 | 100.0% | genre ID (structural) |
| feature categories | 123,894 | 100.0% | single-player/co-op/cloud/... flags |
| store_tags | 123,852 | 99.9% | ranked community tag IDs |
| genres | 123,789 | 99.9% | genre IDs |
| supported languages | 123,847 | 99.9% | language codes |
| developer | 123,747 | 99.9% | structured dev name(s) |
| publisher | 123,460 | 99.6% | structured publisher name(s) |
| release date | 122,308 | 98.7% | PICS steam_release_date |
| release state | 122,464 | 98.8% | released/prerelease (structural) |
| review score | 79,165 | 63.9% | Valve bucket + %positive (review-floor bound) |
| full-audio languages | 54,499 | 44.0% | carriers only |
| controller support | 42,431 | 34.2% | full/partial (from cats 28/18) |
| Steam Deck compat | 33,729 | 27.2% | Deck-rated titles only |
| franchise | 29,142 | 23.5% | structured franchise name(s) |
| content descriptors | 28,091 | 22.7% | mature flags (adult gate = code 4) |
| Early Access (genre-70) | 11,827 | 9.5% | Valve EA signal (not the SteamSpy tag) |
| AI content disclosure | 12,797 | 10.3% | aicontenttype 1/2 carriers only |
| custom EULA | 11,003 | 8.9% | carriers only |
| original release date | 11,213 | 9.0% | EA->1.0 carriers only |
| metacritic | 4,068 | 3.3% | carriers only |
| family-share excluded | 837 | 0.7% | excluded titles only |
| review-bomb adjusted | 97 | 0.1% | bombed titles only |

**AI content disclosure.** 12,797 games carry an `aicontenttype` flag (10.3% of catalog): **12,387 pre-generated** (shipped assets made with AI) and **410 live-generated** (runtime generation). The other ~90% of `pics/` records carry no disclosure flag. PICS gives the typed flag + category only; the free-text blurb lives on the store page.

**Steam Deck.** Of the 33,729 Deck-rated titles: **8,126 Verified**, **19,448 Playable**, **6,155 Unsupported**. The long tail of the catalog is simply unrated by Valve (no Deck category), which is why Deck coverage is ~27% of `pics/`, not the ~97% the AAA-heavy probe sample suggested.

---

## Notes

**Prices.** `prices.json` holds **105,791 rows (85.3% of catalog)**, tracking the non-free base (**105,815** games after removing 18,155 `is_free`). **14,182 of 105,791 priced titles (13.4%) are currently discounted**; sale end-dates come from the `IStoreBrowseService/GetItems` pass.

**Playtime backfill.** Raw playtime covers **78,740 games (63.5% of catalog)**. The honest denominator is the addressable set after the `MIN_REVIEWS_FLOOR = 10` gate — **78,838 games (63.6% of catalog)** — against which raw is **99.9% of addressable**. The other **45,132 games are below the floor and correctly skipped** (too few reviews to produce a sentiment-split median).

**Summarizers run on every raw pass.** Raw 78,740 · summarized 78,569 · rated 78,563 (current gap 171). `playtime_summarize.py` and `ratings_summarize.py` are chained into `playtime-raw.yml`, so the small frontend files refresh right after the shards do (8×/day). Any residual gap is just within-run ordering between the shard commit and the summarize steps, not a stalled pipeline.

**Update events.** `updates.json` (event_type-based patch history) covers **45,402 games (36.6%)**, built from **57 of 57 `updates_raw/` shards populated**. This layer feeds the frontend's **Updated column cadence badge** (`N · 90d` / `N · 1y`, from the summed 90d/365d `counts`) and backfills a null News-API `last_update_ts`. It is gated by the same 10-review floor as playtime. As the remaining shards populate this coverage rises; the precedence flip (event layer becomes primary over News-API) is gated on that coverage — see ARCHITECTURE §9.5 / §3.1.

**HLTB estimates refresh live.** **18,822 of 123,948 `hltb.json` entries carry the `est` flag** (15.2%), leaving **105,126 reals (84.8%)**. `hltb_estimate.py` is a shared helper imported by `hltb_refresh.py` (as `HE`) and called in the fill loop, so estimates recompute from live median ratios every 2h as new reals land — not a stale one-off.

**Rating % / review count.** Rating % is present for **112,544 games (90.8%)**; review count for **115,859 (93.5%)**. The gap is titles with too few reviews to carry a meaningful score.

**Tags** are non-empty for **123,914 games (100.0%)**. The remainder are untagged on SteamSpy (typically very-low-review or unreleased titles).

---

## Future work

- **Tag refresh schedule.** `tags.json` currently has no per-entry timestamp and no rescrape cadence — tags are fetched once and left. Tags do drift (Steam user-tag votes shift, new tags get added), so a light periodic re-check (e.g. weekly/monthly, oldest-first, small budget) is worth adding. That needs a per-entry `scraped_at` in `tags.json` first, after which tags can join Axis 2 as a proper row here.
- **Uniform-refresh review.** Once the fill frontier drains (near-100% coverage), revisit whether the dormant 30–45d cooldowns should tighten toward a flatter, faster whole-catalog refresh. Today's split (active 4–7d / dormant 30–45d) deliberately front-loads budget onto games whose data actually moves; a uniform target only makes sense once no backlog is competing for that budget.
- **Playtime per-game timestamp.** Adding a per-game `scraped_at` to the `playtime_raw/` shard records would replace the newest-review-`ts` proxy with an exact staleness signal, removing the (approx) caveat above.

