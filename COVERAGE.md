# SteamQHPP — Data Coverage Snapshot

> Generated by `coverage.py` after each scrape. Do not edit by hand — changes will be overwritten on the next run. Design rationale for both axes is in ARCHITECTURE §11.5.

**Snapshot generated (UTC):** 2026-07-22 02:51
**Base universe (`games.json`):** 124,134 games

Per-file generation timestamps at snapshot time:

| File | Generated (UTC) |
|---|---|
| `games.json` | 2026-07-22 02:45 |
| `prices.json` | 2026-07-21 22:59 |
| `hltb.json` | 2026-07-22 02:33 |
| `recent.json` | 2026-07-22 00:19 |
| `playtime.json` | 2026-07-21 23:26 |
| `ratings.json` | 2026-07-21 23:27 |
| `tags.json` | 2026-07-21 20:05 |
| `updates.json` | 2026-07-21 22:32 |
| `playtime_raw/` (shards) | newest 2026-07-21 23:26 · oldest 2026-07-13 19:43 |
| `updates_raw/` (64/64 populated) | newest 2026-07-21 22:32 · oldest 2026-07-08 07:05 |
| `pics_raw/` (64/64 populated) | newest 2026-07-21 09:49 · oldest 2026-07-21 09:15 |

---

## Axis 1 — Coverage by metric (sorted by % of catalog, descending)

How much of the catalog we hold for each metric. "Have we got it?"

| Metric | Storage file | Covered | % of catalog | Missing |
|---|---|---:|---:|---:|
| Release date | `games.json` | 124,132 | 100.0% | 2 |
| Recent reviews | `recent.json` | 124,120 | 100.0% | 14 |
| HLTB (total) | `hltb.json` | 124,120 | 100.0% | 14 |
| Tags (non-empty) | `tags.json` | 124,086 | 100.0% | 48 |
| PICS metadata (raw) | `pics_raw/` | 124,028 | 99.9% | 106 |
| PICS metadata (summ.) | `pics/` | 124,028 | 99.9% | 106 |
| Review count | `games.json` | 115,932 | 93.4% | 8,202 |
| Rating % | `games.json` | 112,613 | 90.7% | 11,521 |
| Price / Sales | `prices.json` | 105,950 | 85.4% | — |
| HLTB (real) | `hltb.json` | 105,291 | 84.8% | — |
| Playtime raw (shards) | `playtime_raw/` | 78,781 | 63.5% | 45,353 |
| Update events (raw) | `updates_raw/` | 78,646 | 63.4% | 45,488 |
| Playtime (summarized) | `playtime.json` | 78,617 | 63.3% | 45,517 |
| Playtime-weighted rating | `ratings.json` | 78,611 | 63.3% | 45,523 |
| Update events (summ.) | `updates.json` | 51,053 | 41.1% | 73,081 |
| HLTB (estimated) | `hltb.json` | 18,829 | 15.2% | — |

Integrity: every storage file is effectively a clean subset of `games.json` (a handful of orphan keys can appear as timing artifacts between scraper commits).

---

## Axis 2 — Refresh schedule (is the pipeline keeping up?)

Covered rows split by how each row's **own scraper** will treat it on the next pass — NOT against a flat target. The **7d (active)** lane is games updated within 90d (short cooldown); the **30d (dormant)** lane is everyone else (long cooldown, refreshed rarely *by design*). Track totals **include** their overdue members. **overdue** = already past its lane's cooldown = real backlog. **never** = no data yet = fill frontier. **empty** = correctly skipped (below the 10-review floor / null score), not pending work.

| Metric | Storage | 7d-track | 30d-track | overdue | empty | never |
|---|---|---:|---:|---:|---:|---:|
| Recent reviews | `recent.json` | 10,835 | 113,285 | 694 | 0 | 14 |
| Playtime raw (approx) | `playtime_raw/` | 7,219 | 71,402 | 49,777 † | 45,255 | 258 |
| Update events | `updates_raw/` | 7,152 | 71,494 | 3,748 | 45,255 | 233 |

**†  Playtime `overdue` is proxy-inflated — read with caution.** `playtime_raw/` stores no per-game scrape timestamp, so staleness uses each game's **newest review `ts`** as a proxy. A game with no recent reviews reads as overdue even if the scraper walked it days ago, so this figure is an **upper bound**, not true backlog (contrast Update events, which has a real per-game `scraped_at` and shows exact overdue). The `empty` column is the 45,255 games below the 10-review floor (correctly skipped, not backlog). An exact figure needs a per-game `scraped_at` in the shards — see Future work.

Files with a single-window (not two-track) refresh rule:

- **`games.json` core** (`scraper.py`): fresh 2,545 · overdue 121,589 · never 0. Refresh is driven by Steam's `last_modified` in Actions; the overdue count here is the local fallback-timer view (≥7d since `scraped_at`).
- **`hltb.json`** (`hltb_refresh.py`): fresh 33,396 · overdue 58 · blank-frozen 0 · blank-active 90,666. Windows: partial 14d, full 365d; blanks back off 3→30→180d by attempt count.
- **`prices.json`** (`price_and_sale.py`): no cooldown gate — the whole non-free base is re-batched every 3h, so there is no meaningful staleness backlog to bucket.
- **`tags.json`** (`tags_refresh.py`): **no timestamp stored and no rescrape schedule** — coverage-only. See Future work below.
- **`playtime.json` / `ratings.json`**: derived from `playtime_raw/` on every raw pass; staleness inherits from the raw shard row above.
- **`pics_raw/`** (`pics_refresh.py`): fresh 124,028 · overdue 0 · never 106. Single flat `--stale-days 14` window (daily cron), and — unlike playtime — keyed on a **real per-game `_ts` fetch stamp**, so overdue here is exact, not a proxy. `never` = catalog games not yet in `pics_raw/` (the fill frontier). `pics/` is derived from `pics_raw/` by `pics_summarize.py` on every pass, so its staleness inherits from this row.

---

## PICS metadata — sub-metric coverage

Fill rates **within the 124,028 summarized `pics/` records** (not against the full catalog). Structural fields (tags, genres, categories, languages, dev/pub) sit near 100%; the sparse rows below them are **by-design carrier-only** fields — their % is a real-world incidence rate (how many games actually carry an AI disclosure, a Deck rating, a review-bomb adjustment, …), **not** a pipeline gap. These figures are live over the full library — they supersede the 120-game probe sample in `PICS_METADATA_PIPELINE.md §2`.

| Sub-metric | Covered | % of `pics/` | Note |
|---|---:|---:|---|
| primary_genre | 124,028 | 100.0% | genre ID (structural) |
| feature categories | 124,008 | 100.0% | single-player/co-op/cloud/... flags |
| store_tags | 123,966 | 100.0% | ranked community tag IDs |
| genres | 123,901 | 99.9% | genre IDs |
| supported languages | 123,961 | 99.9% | language codes |
| developer | 123,861 | 99.9% | structured dev name(s) |
| publisher | 123,571 | 99.6% | structured publisher name(s) |
| release date | 122,456 | 98.7% | PICS steam_release_date |
| release state | 122,578 | 98.8% | released/prerelease (structural) |
| review score | 79,331 | 64.0% | Valve bucket + %positive (review-floor bound) |
| full-audio languages | 54,533 | 44.0% | carriers only |
| controller support | 42,478 | 34.2% | full/partial (from cats 28/18) |
| Steam Deck compat | 33,847 | 27.3% | Deck-rated titles only |
| franchise | 29,166 | 23.5% | structured franchise name(s) |
| content descriptors | 28,113 | 22.7% | mature flags (adult gate = code 4) |
| Early Access (genre-70) | 11,812 | 9.5% | Valve EA signal (not the SteamSpy tag) |
| AI content disclosure | 12,854 | 10.4% | aicontenttype 1/2 carriers only |
| custom EULA | 11,014 | 8.9% | carriers only |
| original release date | 11,213 | 9.0% | EA->1.0 carriers only |
| metacritic | 4,070 | 3.3% | carriers only |
| family-share excluded | 821 | 0.7% | excluded titles only |
| review-bomb adjusted | 97 | 0.1% | bombed titles only |

**AI content disclosure.** 12,854 games carry an `aicontenttype` flag (10.4% of catalog): **12,444 pre-generated** (shipped assets made with AI) and **410 live-generated** (runtime generation). The other ~90% of `pics/` records carry no disclosure flag. PICS gives the typed flag + category only; the free-text blurb lives on the store page.

**Steam Deck.** Of the 33,847 Deck-rated titles: **8,154 Verified**, **19,524 Playable**, **6,169 Unsupported**. The long tail of the catalog is simply unrated by Valve (no Deck category), which is why Deck coverage is ~27% of `pics/`, not the ~97% the AAA-heavy probe sample suggested.

---

## Notes

**Prices.** `prices.json` holds **105,950 rows (85.4% of catalog)**, tracking the non-free base (**105,966** games after removing 18,168 `is_free`). **12,473 of 105,950 priced titles (11.8%) are currently discounted**; sale end-dates come from the `IStoreBrowseService/GetItems` pass.

**Playtime backfill.** Raw playtime covers **78,781 games (63.5% of catalog)**. The honest denominator is the addressable set after the `MIN_REVIEWS_FLOOR = 10` gate — **78,879 games (63.5% of catalog)** — against which raw is **99.9% of addressable**. The other **45,255 games are below the floor and correctly skipped** (too few reviews to produce a sentiment-split median).

**Summarizers run on every raw pass.** Raw 78,781 · summarized 78,617 · rated 78,611 (current gap 164). `playtime_summarize.py` and `ratings_summarize.py` are chained into `playtime-raw.yml`, so the small frontend files refresh right after the shards do (8×/day). Any residual gap is just within-run ordering between the shard commit and the summarize steps, not a stalled pipeline.

**Update events.** `updates.json` (event_type-based patch history) covers **51,053 games (41.1%)**, built from **64 of 64 `updates_raw/` shards populated**. This layer feeds the frontend's **Updated column cadence badge** (`N · 90d` / `N · 1y`, from the summed 90d/365d `counts`) and backfills a null News-API `last_update_ts`. It is gated by the same 10-review floor as playtime. As the remaining shards populate this coverage rises; the precedence flip (event layer becomes primary over News-API) is gated on that coverage — see ARCHITECTURE §9.5 / §3.1.

**HLTB estimates refresh live.** **18,829 of 124,120 `hltb.json` entries carry the `est` flag** (15.2%), leaving **105,291 reals (84.8%)**. `hltb_estimate.py` is a shared helper imported by `hltb_refresh.py` (as `HE`) and called in the fill loop, so estimates recompute from live median ratios every 2h as new reals land — not a stale one-off.

**Rating % / review count.** Rating % is present for **112,613 games (90.7%)**; review count for **115,932 (93.4%)**. The gap is titles with too few reviews to carry a meaningful score.

**Tags** are non-empty for **124,086 games (100.0%)**. The remainder are untagged on SteamSpy (typically very-low-review or unreleased titles).

---

## Future work

- **Tag refresh schedule.** `tags.json` currently has no per-entry timestamp and no rescrape cadence — tags are fetched once and left. Tags do drift (Steam user-tag votes shift, new tags get added), so a light periodic re-check (e.g. weekly/monthly, oldest-first, small budget) is worth adding. That needs a per-entry `scraped_at` in `tags.json` first, after which tags can join Axis 2 as a proper row here.
- **Uniform-refresh review.** Once the fill frontier drains (near-100% coverage), revisit whether the dormant 30–45d cooldowns should tighten toward a flatter, faster whole-catalog refresh. Today's split (active 4–7d / dormant 30–45d) deliberately front-loads budget onto games whose data actually moves; a uniform target only makes sense once no backlog is competing for that budget.
- **Playtime per-game timestamp.** Adding a per-game `scraped_at` to the `playtime_raw/` shard records would replace the newest-review-`ts` proxy with an exact staleness signal, removing the (approx) caveat above.

