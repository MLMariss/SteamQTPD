# SteamQHPP — Data Coverage Snapshot

> Generated by `coverage.py` after each scrape. Do not edit by hand — changes will be overwritten on the next run. Design rationale for both axes is in ARCHITECTURE §11.5.

**Snapshot generated (UTC):** 2026-09-11 04:59
**Base universe (`games.json`):** 129,139 games

Per-file generation timestamps at snapshot time:

| File | Generated (UTC) |
|---|---|
| `games.json` | 2026-09-11 04:45 |
| `prices.json` | 2026-09-11 00:08 |
| `hltb.json` | 2026-09-11 00:56 |
| `recent.json` | 2026-09-11 00:51 |
| `playtime.json` | 2026-09-11 03:15 |
| `ratings.json` | 2026-09-11 03:16 |
| `tags.json` | 2026-09-11 00:33 |
| `updates.json` | 2026-09-10 23:41 |
| `playtime_raw/` (shards) | newest 2026-09-11 03:15 · oldest 2026-09-11 00:32 |
| `updates_raw/` (64/64 populated) | newest 2026-09-10 23:41 · oldest 2026-08-24 16:13 |
| `pics_raw/` (64/64 populated) | newest 2026-09-10 23:29 · oldest 2026-09-09 08:15 |

---

## Axis 1 — Coverage by metric (sorted by % of catalog, descending)

How much of the catalog we hold for each metric. "Have we got it?"

| Metric | Storage file | Covered | % of catalog | Missing |
|---|---|---:|---:|---:|
| Release date | `games.json` | 129,137 | 100.0% | 2 |
| Recent reviews | `recent.json` | 129,123 | 100.0% | 16 |
| HLTB (total) | `hltb.json` | 129,123 | 100.0% | 16 |
| PICS metadata (raw) | `pics_raw/` | 129,109 | 100.0% | 30 |
| PICS metadata (summ.) | `pics/` | 129,109 | 100.0% | 30 |
| Tags (non-empty) | `tags.json` | 129,088 | 100.0% | 51 |
| Review count | `games.json` | 120,787 | 93.5% | 8,352 |
| Rating % | `games.json` | 117,283 | 90.8% | 11,856 |
| Price / Sales | `prices.json` | 110,439 | 85.5% | — |
| HLTB (real) | `hltb.json` | 110,087 | 85.2% | — |
| Playtime raw (shards) | `playtime_raw/` | 84,208 | 65.2% | 44,931 |
| Playtime (summarized) | `playtime.json` | 84,195 | 65.2% | 44,944 |
| Playtime-weighted rating | `ratings.json` | 84,188 | 65.2% | 44,951 |
| Update events (raw) | `updates_raw/` | 81,331 | 63.0% | 47,808 |
| Update events (summ.) | `updates.json` | 53,219 | 41.2% | 75,920 |
| HLTB (estimated) | `hltb.json` | 19,036 | 14.7% | — |

Integrity: every storage file is effectively a clean subset of `games.json` (a handful of orphan keys can appear as timing artifacts between scraper commits).

---

## Axis 2 — Refresh schedule (is the pipeline keeping up?)

Covered rows split by how each row's **own scraper** will treat it on the next pass — NOT against a flat target. The **7d (active)** lane is games updated within 90d (short cooldown); the **30d (dormant)** lane is everyone else (long cooldown, refreshed rarely *by design*). Track totals **include** their overdue members. **overdue** = already past its lane's cooldown = real backlog. **never** = no data yet = fill frontier. **empty** = correctly skipped (below the row's own review floor / null score), not pending work.

| Metric | Storage | 7d-track | 30d-track | overdue | empty | never |
|---|---|---:|---:|---:|---:|---:|
| Recent reviews | `recent.json` | 11,922 | 117,201 | 5 | 0 | 16 |
| Update events | `updates_raw/` | 7,658 | 73,629 | 78,232 | 47,426 | 426 |

Playtime is **not** a two-track row — every game gets its own cooldown from `playtime_refresh.cooldown_days()` — so it gets its own table below. The `empty` column above is per-row: 47,426 games sit below Update events' 10-review floor. Playtime's own floor is 5 — the point at which a sentiment-split median first becomes mathematically guaranteed.

### Playtime raw — surface refresh (`playtime_raw/`)

Staleness is the **real per-game `scraped_at`**, measured against the real ladder (release-age tiers <7d→1d · <30d→3d · <90d→7d · else 30d; halved above 1,000 reviews, then floored by the popularity tiers >1000→5d/>500→10d). Because the cooldown varies per game, **promise** is the range actually applied within each release-age tier. `covered` 84,197 · `overdue` 31,094 · `never` 13,079 · `empty` 31,863.

| Release age | n | Promise | Median stale | p90 | Inside window |
|---|---:|---:|---:|---:|---:|
| 0–7 d | 162 | 0.5–1 d | 0.7 d | 2.3 d | 75.9% |
| 7–30 d | 1,222 | 1.5–3 d | 1.9 d | 5.4 d | 58.8% |
| 30–90 d | 2,738 | 3.5–7 d | 3.9 d | 9.3 d | 78.5% |
| 90 d+ | 80,075 | 5–30 d | 25.6 d | 33.5 d | 62.6% |

**A surface refresh is not a full one.** For a game already holding 3,000 reviews a normal visit stops after page 1, so only its newest ~100 playtimes are re-read; positions below that stay frozen at whatever they were when first captured. Small games (below their rung) are walked to the end of Steam's list every visit, so for them this table *is* the whole story.

### Playtime raw — deep re-walk (drift control)

The only axis that unfreezes a ceiling game's older playtimes is a FULL window re-walk, stamped `walk_at` and promised every 30 d. This is the number that answers *are our medians drifting as reviewers keep playing*.

| Metric | Value |
|---|---:|
| Games at the 3,000-review ceiling | 3,786 |
| — anchored (deep clock running) | 3,786 |
| — **no `walk_at` anchor** (never deep-walked) | **0** (0.0%) |
| Anchored: median since last full walk | 6.1 d |
| Anchored: p90 | 34.7 d |
| Anchored: max | 36.7 d |
| Anchored: past the 30 d promise | 693 (18.3%) |


Files with a single-window (not two-track) refresh rule:

- **`games.json` core** (`scraper.py`): fresh 25,469 · overdue 891 · never 0 · last-modified-only 102,779. Games within 365d of release are on the age-tiered review refresh (≤3d→0.25d, ≤10d→0.5d, ≤30d→1d, ≤60d→2d, ≤90d→3.5d, ≤180d→7d, ≤365d→15d), so fresh/overdue are exact for them. Older games refresh on Steam's `last_modified` (invisible locally) plus the PICS review-drift trigger, and are counted separately rather than scored against a cooldown they don't have.
- **`hltb.json`** (`hltb_refresh.py`): fresh 33,010 · overdue 1,193 · blank-frozen 94,837 · blank-active 83. Windows: partial 14d, full 365d; blanks back off 3→30→180d by attempt count.
- **`prices.json`** (`price_and_sale.py`): no cooldown gate — the whole non-free base is re-batched every 3h, so there is no meaningful staleness backlog to bucket.
- **`tags.json`** (`tags_refresh.py`): **no timestamp stored and no rescrape schedule** — coverage-only. See Future work below.
- **`playtime.json` / `ratings.json`**: derived from `playtime_raw/` on every raw pass; staleness inherits from the raw shard row above.
- **`pics_raw/`** (`pics_refresh.py`): fresh 128,562 · overdue 547 · never 30. Single flat `--stale-days 14` window (daily cron), and — unlike playtime — keyed on a **real per-game `_ts` fetch stamp**, so overdue here is exact, not a proxy. `never` = catalog games not yet in `pics_raw/` (the fill frontier). `pics/` is derived from `pics_raw/` by `pics_summarize.py` on every pass, so its staleness inherits from this row.

---

## PICS metadata — sub-metric coverage

Fill rates **within the 129,109 summarized `pics/` records** (not against the full catalog). Structural fields (tags, genres, categories, languages, dev/pub) sit near 100%; the sparse rows below them are **by-design carrier-only** fields — their % is a real-world incidence rate (how many games actually carry an AI disclosure, a Deck rating, a review-bomb adjustment, …), **not** a pipeline gap. These figures are live over the full library — they supersede the 120-game probe sample in `PICS_METADATA_PIPELINE.md §2`.

| Sub-metric | Covered | % of `pics/` | Note |
|---|---:|---:|---|
| primary_genre | 129,105 | 100.0% | genre ID (structural) |
| feature categories | 129,086 | 100.0% | single-player/co-op/cloud/... flags |
| store_tags | 129,045 | 100.0% | ranked community tag IDs |
| store header art | 129,057 | 100.0% | capsule/header path (structural — blank thumbnails without it) |
| genres | 128,981 | 99.9% | genre IDs |
| supported languages | 129,040 | 99.9% | language codes |
| developer | 128,937 | 99.9% | structured dev name(s) |
| publisher | 128,641 | 99.6% | structured publisher name(s) |
| release date | 127,118 | 98.5% | PICS steam_release_date |
| release state | 127,659 | 98.9% | released/prerelease (structural) |
| review score | 81,571 | 63.2% | Valve bucket + %positive (review-floor bound) |
| full-audio languages | 56,375 | 43.7% | carriers only |
| controller support | 44,170 | 34.2% | full/partial (from cats 28/18) |
| Steam Deck compat | 34,803 | 27.0% | Deck-rated titles only |
| franchise | 30,081 | 23.3% | structured franchise name(s) |
| content descriptors | 29,235 | 22.6% | mature flags (adult gate = code 3 or 4) |
| Early Access (genre-70) | 12,288 | 9.5% | Valve EA signal (not the SteamSpy tag) |
| AI content disclosure | 15,099 | 11.7% | aicontenttype 1/2 carriers only |
| custom EULA | 11,282 | 8.7% | carriers only |
| original release date | 11,216 | 8.7% | EA->1.0 carriers only |
| metacritic | 4,107 | 3.2% | carriers only |
| family-share excluded | 743 | 0.6% | excluded titles only |
| review-bomb adjusted | 103 | 0.1% | bombed titles only |

**AI content disclosure.** 15,099 games carry an `aicontenttype` flag (11.7% of catalog): **14,625 pre-generated** (shipped assets made with AI) and **474 live-generated** (runtime generation). The other ~88% of `pics/` records carry no disclosure flag. PICS gives the typed flag + category only; the free-text blurb lives on the store page.

**Steam Deck.** Of the 34,803 Deck-rated titles: **8,492 Verified**, **20,056 Playable**, **6,255 Unsupported**. The long tail of the catalog is simply unrated by Valve (no Deck category), which is why Deck coverage is ~27% of `pics/`, not the ~97% the AAA-heavy probe sample suggested.

---

## Notes

**Prices.** `prices.json` holds **110,439 rows (85.5% of catalog)**, tracking the non-free base (**110,461** games after removing 18,678 `is_free`). **9,788 of 110,439 priced titles (8.9%) are currently discounted**; sale end-dates come from the `IStoreBrowseService/GetItems` pass.

**Playtime backfill.** Raw playtime covers **84,208 games (65.2% of catalog)**. The honest denominator is the addressable set after the `MIN_REVIEWS_FLOOR = 5` gate — **97,276 games (75.3% of catalog)** — against which raw is **86.6% of addressable**. The other **31,863 games are below the floor and correctly skipped** (too few reviews to produce a sentiment-split median).

**Summarizers run on every raw pass.** Raw 84,208 · summarized 84,195 · rated 84,188 (current gap 13). `playtime_summarize.py` and `ratings_summarize.py` are chained into `playtime-raw.yml`, so the small frontend files refresh right after the shards do (8×/day). Any residual gap is just within-run ordering between the shard commit and the summarize steps, not a stalled pipeline.

**Update events.** `updates.json` (event_type-based patch history) covers **53,219 games (41.2%)**, built from **64 of 64 `updates_raw/` shards populated**. This layer feeds the frontend's **Updated column cadence badge** (`N · 90d` / `N · 1y`, from the summed 90d/365d `counts`) and backfills a null News-API `last_update_ts`. It is gated by its own 10-review floor (playtime's is 5; the two no longer match). As the remaining shards populate this coverage rises; the precedence flip (event layer becomes primary over News-API) is gated on that coverage — see ARCHITECTURE §9.5 / §3.1.

**HLTB estimates refresh live.** **19,036 of 129,123 `hltb.json` entries carry the `est` flag** (14.7%), leaving **110,087 reals (85.2%)**. `hltb_estimate.py` is a shared helper imported by `hltb_refresh.py` (as `HE`) and called in the fill loop, so estimates recompute from live median ratios every 2h as new reals land — not a stale one-off.

**Rating % / review count.** Rating % is present for **117,283 games (90.8%)**; review count for **120,787 (93.5%)**. The gap is titles with too few reviews to carry a meaningful score.

**Tags** are non-empty for **129,088 games (100.0%)**. The remainder are untagged on SteamSpy (typically very-low-review or unreleased titles).

---

## Future work

- **Tag refresh schedule.** `tags.json` currently has no per-entry timestamp and no rescrape cadence — tags are fetched once and left. Tags do drift (Steam user-tag votes shift, new tags get added), so a light periodic re-check (e.g. weekly/monthly, oldest-first, small budget) is worth adding. That needs a per-entry `scraped_at` in `tags.json` first, after which tags can join Axis 2 as a proper row here.
- **Uniform-refresh review.** Once the fill frontier drains (near-100% coverage), revisit whether the dormant 30–45d cooldowns should tighten toward a flatter, faster whole-catalog refresh. Today's split (active 4–7d / dormant 30–45d) deliberately front-loads budget onto games whose data actually moves; a uniform target only makes sense once no backlog is competing for that budget.

