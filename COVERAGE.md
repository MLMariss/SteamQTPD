# SteamQHPP — Data Coverage Snapshot

> Generated by `coverage.py` after each scrape. Do not edit by hand — changes will be overwritten on the next run. Design rationale for both axes is in ARCHITECTURE §11.5.

**Snapshot generated (UTC):** 2026-09-13 05:24
**Base universe (`games.json`):** 129,295 games

Per-file generation timestamps at snapshot time:

| File | Generated (UTC) |
|---|---|
| `games.json` | 2026-09-13 05:06 |
| `prices.json` | 2026-09-13 05:06 |
| `hltb.json` | 2026-09-13 01:03 |
| `recent.json` | 2026-09-13 00:34 |
| `playtime.json` | 2026-09-13 03:30 |
| `ratings.json` | 2026-09-13 03:30 |
| `tags.json` | 2026-09-12 22:38 |
| `updates.json` | 2026-09-12 23:30 |
| `playtime_raw/` (shards) | newest 2026-09-13 03:29 · oldest 2026-09-12 19:30 |
| `updates_raw/` (64/64 populated) | newest 2026-09-12 23:30 · oldest 2026-08-26 16:56 |
| `pics_raw/` (64/64 populated) | newest 2026-09-12 23:30 · oldest 2026-09-12 07:58 |

---

## Axis 1 — Coverage by metric (sorted by % of catalog, descending)

How much of the catalog we hold for each metric. "Have we got it?"

| Metric | Storage file | Covered | % of catalog | Missing |
|---|---|---:|---:|---:|
| Release date | `games.json` | 129,293 | 100.0% | 2 |
| Recent reviews | `recent.json` | 129,290 | 100.0% | 5 |
| HLTB (total) | `hltb.json` | 129,290 | 100.0% | 5 |
| PICS metadata (raw) | `pics_raw/` | 129,276 | 100.0% | 19 |
| PICS metadata (summ.) | `pics/` | 129,276 | 100.0% | 19 |
| Tags (non-empty) | `tags.json` | 129,255 | 100.0% | 40 |
| Review count | `games.json` | 120,941 | 93.5% | 8,354 |
| Rating % | `games.json` | 117,425 | 90.8% | 11,870 |
| HLTB (real) | `hltb.json` | 110,261 | 85.3% | — |
| Playtime raw (shards) | `playtime_raw/` | 89,625 | 69.3% | 39,670 |
| Playtime (summarized) | `playtime.json` | 89,612 | 69.3% | 39,683 |
| Playtime-weighted rating | `ratings.json` | 89,598 | 69.3% | 39,697 |
| Update events (raw) | `updates_raw/` | 81,435 | 63.0% | 47,860 |
| Update events (summ.) | `updates.json` | 53,308 | 41.2% | 75,987 |
| Price / Sales | `prices.json` | 29,497 | 22.8% | — |
| HLTB (estimated) | `hltb.json` | 19,029 | 14.7% | — |

Integrity: every storage file is effectively a clean subset of `games.json` (a handful of orphan keys can appear as timing artifacts between scraper commits).

---

## Axis 2 — Refresh schedule (is the pipeline keeping up?)

Covered rows split by how each row's **own scraper** will treat it on the next pass — NOT against a flat target. The **7d (active)** lane is games updated within 90d (short cooldown); the **30d (dormant)** lane is everyone else (long cooldown, refreshed rarely *by design*). Track totals **include** their overdue members. **overdue** = already past its lane's cooldown = real backlog. **never** = no data yet = fill frontier. **empty** = correctly skipped (below the row's own review floor / null score), not pending work.

| Metric | Storage | 7d-track | 30d-track | overdue | empty | never |
|---|---|---:|---:|---:|---:|---:|
| Recent reviews | `recent.json` | 11,922 | 117,368 | 525 | 0 | 5 |
| Update events | `updates_raw/` | 7,697 | 73,693 | 78,297 | 47,501 | 404 |

Playtime is **not** a two-track row — every game gets its own cooldown from `playtime_refresh.cooldown_days()` — so it gets its own table below. The `empty` column above is per-row: 47,501 games sit below Update events' 10-review floor. Playtime's own floor is 5 — the point at which a sentiment-split median first becomes mathematically guaranteed.

### Playtime raw — surface refresh (`playtime_raw/`)

Staleness is the **real per-game `scraped_at`**, measured against the real ladder (release-age tiers <7d→1d · <30d→3d · <90d→7d · else 30d; halved above 1,000 reviews, then floored by the popularity tiers >1000→5d/>500→10d). Because the cooldown varies per game, **promise** is the range actually applied within each release-age tier. `covered` 89,611 · `overdue` 33,163 · `never` 7,768 · `empty` 31,916.

| Release age | n | Promise | Median stale | p90 | Inside window |
|---|---:|---:|---:|---:|---:|
| 0–7 d | 218 | 0.5–1 d | 2.0 d | 3.9 d | 33.9% |
| 7–30 d | 1,075 | 1.5–3 d | 3.2 d | 7.4 d | 46.4% |
| 30–90 d | 2,877 | 3.5–7 d | 4.2 d | 10.9 d | 71.8% |
| 90 d+ | 85,441 | 5–30 d | 6.2 d | 35.5 d | 63.0% |

**A surface refresh is not a full one.** For a game already holding 3,000 reviews a normal visit stops after page 1, so only its newest ~100 playtimes are re-read; positions below that stay frozen at whatever they were when first captured. Small games (below their rung) are walked to the end of Steam's list every visit, so for them this table *is* the whole story.

### Playtime raw — deep re-walk (drift control)

The only axis that unfreezes a ceiling game's older playtimes is a FULL window re-walk, stamped `walk_at` and promised every 30 d. This is the number that answers *are our medians drifting as reviewers keep playing*.

| Metric | Value |
|---|---:|
| Games at the 3,000-review ceiling | 3,832 |
| — anchored (deep clock running) | 3,832 |
| — **no `walk_at` anchor** (never deep-walked) | **0** (0.0%) |
| Anchored: median since last full walk | 7.7 d |
| Anchored: p90 | 36.6 d |
| Anchored: max | 38.8 d |
| Anchored: past the 30 d promise | 608 (15.9%) |


Files with a single-window (not two-track) refresh rule:

- **`games.json` core** (`scraper.py`): fresh 26,078 · overdue 358 · never 0 · last-modified-only 102,859. Games within 365d of release are on the age-tiered review refresh (≤3d→0.25d, ≤10d→0.5d, ≤30d→1d, ≤60d→2d, ≤90d→3.5d, ≤180d→7d, ≤365d→15d), so fresh/overdue are exact for them. Older games refresh on Steam's `last_modified` (invisible locally) plus the PICS review-drift trigger, and are counted separately rather than scored against a cooldown they don't have.
- **`hltb.json`** (`hltb_refresh.py`): fresh 33,901 · overdue 320 · blank-frozen 95,052 · blank-active 17. Windows: partial 14d, full 365d; blanks back off 3→30→180d by attempt count.
- **`prices.json`** (`price_and_sale.py`): no cooldown gate — the whole non-free base is re-batched every 3h, so there is no meaningful staleness backlog to bucket.
- **`tags.json`** (`tags_refresh.py`): **no timestamp stored and no rescrape schedule** — coverage-only. See Future work below.
- **`playtime.json` / `ratings.json`**: derived from `playtime_raw/` on every raw pass; staleness inherits from the raw shard row above.
- **`pics_raw/`** (`pics_refresh.py`): fresh 128,819 · overdue 457 · never 19. Single flat `--stale-days 14` window (daily cron), and — unlike playtime — keyed on a **real per-game `_ts` fetch stamp**, so overdue here is exact, not a proxy. `never` = catalog games not yet in `pics_raw/` (the fill frontier). `pics/` is derived from `pics_raw/` by `pics_summarize.py` on every pass, so its staleness inherits from this row.

---

## PICS metadata — sub-metric coverage

Fill rates **within the 129,276 summarized `pics/` records** (not against the full catalog). Structural fields (tags, genres, categories, languages, dev/pub) sit near 100%; the sparse rows below them are **by-design carrier-only** fields — their % is a real-world incidence rate (how many games actually carry an AI disclosure, a Deck rating, a review-bomb adjustment, …), **not** a pipeline gap. These figures are live over the full library — they supersede the 120-game probe sample in `PICS_METADATA_PIPELINE.md §2`.

| Sub-metric | Covered | % of `pics/` | Note |
|---|---:|---:|---|
| primary_genre | 129,272 | 100.0% | genre ID (structural) |
| feature categories | 129,253 | 100.0% | single-player/co-op/cloud/... flags |
| store_tags | 129,212 | 100.0% | ranked community tag IDs |
| store header art | 129,224 | 100.0% | capsule/header path (structural — blank thumbnails without it) |
| genres | 129,148 | 99.9% | genre IDs |
| supported languages | 129,207 | 99.9% | language codes |
| developer | 129,104 | 99.9% | structured dev name(s) |
| publisher | 128,808 | 99.6% | structured publisher name(s) |
| release date | 127,263 | 98.4% | PICS steam_release_date |
| release state | 127,826 | 98.9% | released/prerelease (structural) |
| review score | 81,652 | 63.2% | Valve bucket + %positive (review-floor bound) |
| full-audio languages | 56,430 | 43.7% | carriers only |
| controller support | 44,210 | 34.2% | full/partial (from cats 28/18) |
| Steam Deck compat | 34,851 | 27.0% | Deck-rated titles only |
| franchise | 30,115 | 23.3% | structured franchise name(s) |
| content descriptors | 29,277 | 22.6% | mature flags (adult gate = code 3 or 4) |
| Early Access (genre-70) | 12,302 | 9.5% | Valve EA signal (not the SteamSpy tag) |
| AI content disclosure | 15,176 | 11.7% | aicontenttype 1/2 carriers only |
| custom EULA | 11,285 | 8.7% | carriers only |
| original release date | 11,216 | 8.7% | EA->1.0 carriers only |
| metacritic | 4,110 | 3.2% | carriers only |
| family-share excluded | 743 | 0.6% | excluded titles only |
| review-bomb adjusted | 103 | 0.1% | bombed titles only |

**AI content disclosure.** 15,176 games carry an `aicontenttype` flag (11.7% of catalog): **14,698 pre-generated** (shipped assets made with AI) and **478 live-generated** (runtime generation). The other ~88% of `pics/` records carry no disclosure flag. PICS gives the typed flag + category only; the free-text blurb lives on the store page.

**Steam Deck.** Of the 34,851 Deck-rated titles: **8,507 Verified**, **20,085 Playable**, **6,259 Unsupported**. The long tail of the catalog is simply unrated by Valve (no Deck category), which is why Deck coverage is ~27% of `pics/`, not the ~97% the AAA-heavy probe sample suggested.

---

## Notes

**Prices.** `prices.json` holds **29,497 rows (22.8% of catalog)**, tracking the non-free base (**110,602** games after removing 18,693 `is_free`). **2,531 of 29,497 priced titles (8.6%) are currently discounted**; sale end-dates come from the `IStoreBrowseService/GetItems` pass.

**Playtime backfill.** Raw playtime covers **89,625 games (69.3% of catalog)**. The honest denominator is the addressable set after the `MIN_REVIEWS_FLOOR = 5` gate — **97,379 games (75.3% of catalog)** — against which raw is **92.0% of addressable**. The other **31,916 games are below the floor and correctly skipped** (too few reviews to produce a sentiment-split median).

**Summarizers run on every raw pass.** Raw 89,625 · summarized 89,612 · rated 89,598 (current gap 13). `playtime_summarize.py` and `ratings_summarize.py` are chained into `playtime-raw.yml`, so the small frontend files refresh right after the shards do (8×/day). Any residual gap is just within-run ordering between the shard commit and the summarize steps, not a stalled pipeline.

**Update events.** `updates.json` (event_type-based patch history) covers **53,308 games (41.2%)**, built from **64 of 64 `updates_raw/` shards populated**. This layer feeds the frontend's **Updated column cadence badge** (`N · 90d` / `N · 1y`, from the summed 90d/365d `counts`) and backfills a null News-API `last_update_ts`. It is gated by its own 10-review floor (playtime's is 5; the two no longer match). As the remaining shards populate this coverage rises; the precedence flip (event layer becomes primary over News-API) is gated on that coverage — see ARCHITECTURE §9.5 / §3.1.

**HLTB estimates refresh live.** **19,029 of 129,290 `hltb.json` entries carry the `est` flag** (14.7%), leaving **110,261 reals (85.3%)**. `hltb_estimate.py` is a shared helper imported by `hltb_refresh.py` (as `HE`) and called in the fill loop, so estimates recompute from live median ratios every 2h as new reals land — not a stale one-off.

**Rating % / review count.** Rating % is present for **117,425 games (90.8%)**; review count for **120,941 (93.5%)**. The gap is titles with too few reviews to carry a meaningful score.

**Tags** are non-empty for **129,255 games (100.0%)**. The remainder are untagged on SteamSpy (typically very-low-review or unreleased titles).

---

## Future work

- **Tag refresh schedule.** `tags.json` currently has no per-entry timestamp and no rescrape cadence — tags are fetched once and left. Tags do drift (Steam user-tag votes shift, new tags get added), so a light periodic re-check (e.g. weekly/monthly, oldest-first, small budget) is worth adding. That needs a per-entry `scraped_at` in `tags.json` first, after which tags can join Axis 2 as a proper row here.
- **Uniform-refresh review.** Once the fill frontier drains (near-100% coverage), revisit whether the dormant 30–45d cooldowns should tighten toward a flatter, faster whole-catalog refresh. Today's split (active 4–7d / dormant 30–45d) deliberately front-loads budget onto games whose data actually moves; a uniform target only makes sense once no backlog is competing for that budget.

